# テスト手順

- 狭い単体テスト: `npm run test -- src/integration/upstreamAdapter.test.ts`
- 型検査: `npm run typecheck`
- lint/format検査: `npm run lint`
- 本番ビルド: `npm run build`
- 全体: `npm run verify`
- 上流データ同期を含むリリース検証: `npm run release:prepare`

ローカル導入後、対象ページで「接続済み」表示、対象外URLで非起動、破損した検証対象保存値で安全停止、コンソールに予期しない例外がないことを確認します。ランキング移植後はforkとのfixture同値、キャンセル、古い結果の競合、上流保存値非変更も検証します。

データパックのテストでは、同期済みJSONの受理、既知仕様だけを使う新ポケモンの追加、未知スキルを持つポケモンだけの除外、欠損データへのフォールバック、ネットワーク取得後の一括適用とキャッシュを確認します。

## ブラウザ確認記録

- 2026-09-12: Microsoft Edgeを隔離プロファイルと `--load-extension=dist` で起動し、対象IVページを開いた。
- 2026-09-12: fork版React/MUI画面への置換後、型検査、lint、9単体テスト、自己完結bundleの本番ビルドに成功し、更新済み `dist` を別の隔離Edgeプロファイルで起動した。
- Windows画面操作ランタイムが内部アセットのパス欠落で初期化できず、UI操作とスクリーンショットによる自動受入確認は未完了。
- Google Chromeはこの環境で実行ファイルを検出できなかったため未確認。
- 2026-09-13: 現行サイトの4上段タブに対し、ランキングが5番目へ追加されることをEdgeの実DOMで確認した。
- 2026-09-13: ランキングの「編集」から、ページ再読み込みなしで元ツールのエナジー／パラメーターが選択されることを確認した。遷移前後で選択色と `aria-selected` が単一の上段タブだけに付くことも確認した。
- 2026-09-13: ランキング計算が明示操作でだけ開始され、開始後は条件変更では無効化されず、明示的な置換またはキャンセルでだけ停止する実行判定を単体テストで確認した。
- 2026-09-13: 上流データパック追加後、型検査、lint、21単体テスト、本番ビルドに成功した。最新上流データでは、既知仕様の246件を受理し、未対応スキルを持つMewtwo 1件だけを局所的に除外することを確認した。
- 2026-09-13: Windows画面操作ランタイムがACL初期化エラーで終了したため、この変更後のChrome／Edge実画面確認は手動確認として残した。
- 2026-09-13: 利用者が通常利用しているMicrosoft Edgeへunpacked extensionを読み込み、ランキング、計算条件連携、参照専用ボックスと元ツール準拠のカード表示まで手動受入確認した。
