# 互換性と上流変更への対応

対応条件をURLだけで推定せず、必要なDOM契約、保存スキーマ、データ版を段階的に検査します。検査失敗時は該当機能を停止し、利用者へ更新待ちまたは設定リセットの案内を表示します。保存値を自動修復・上書きしません。

上流更新時はforkの `scripts/upstream-boundary.json` と関連差分を確認し、adapter fixture、domain同値テスト、実ブラウザ疎通を更新します。対応範囲をリリースノートに記録し、緊急時は機能フラグで問題機能だけを既定無効にします。リモート設定やリモートコードには依存しません。
