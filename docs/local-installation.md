# Chrome／Edgeへのローカルインストール

`npm run build` 後、Chromeは `chrome://extensions`、Edgeは `edge://extensions` を開き、デベロッパーモードを有効にして「パッケージ化されていない拡張機能を読み込む」から `dist` を選びます。

`https://nitoyon.github.io/pokesleep-tool/` または `/iv/` を開き、右下に接続表示が出ることを確認します。開発中のため、通常利用では拡張を無効にしてください。
