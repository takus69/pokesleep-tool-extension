# プライバシー方針

拡張は対象ページ上の利用者データを端末内で処理します。初期版は解析、広告、外部送信、外部コード取得を行いません。拡張設定は `chrome.storage.local` に、元ツールの既存データは対象originの保存領域に残ります。

今後権限やデータ取扱いを追加する場合は、実装前に目的、保持期間、共有先、削除方法を本文とストア表示へ反映します。必要最小限を超える権限は要求しません。
