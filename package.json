{
  "name": "pokesleep-tool-extension",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "vite build",
    "typecheck": "tsc --noEmit",
    "lint": "biome check .",
    "test": "vitest run",
    "verify": "npm run typecheck && npm run lint && npm run test && npm run build"
  },
  "devDependencies": {
    "@biomejs/biome": "2.4.13",
    "@types/chrome": "^0.1.31",
    "@types/node": "^24.5.0",
    "jsdom": "26.1.0",
    "typescript": "^5.9.2",
    "vite": "6.4.3",
    "vitest": "3.2.7"
  }
}
